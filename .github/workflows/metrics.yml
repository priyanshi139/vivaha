name: Action Metrics Workflow

on:
  push:
    branches: [ main ]
  schedule:
    - cron: '0 0 * * *' # daily run (optional)

jobs:
  run-metrics:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Run some script
        run: echo "Running metrics collection..."
